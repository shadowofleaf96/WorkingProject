<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Secure Node.js using Passport App</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <div class="second-container">
      <div class="container">
        <h1>Login</h1>
        <div
          class="error-message <%= errors && errors.length ? 'error' : '' %>"
        >
          <% if (errors && errors.length) { %>
          <ul>
            <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
            <% }); %>
          </ul>
          <% } %>
        </div>
        <form class="form" action="/users/login" method="POST">
          <div class="form-group">
            <label for="username">Username</label>
            <input
              type="text"
              id="username"
              name="username"
              placeholder="write admin"
              required
            />
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              placeholder="write admin1234"
              required
            />
          </div>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="form-group">
            <button class="edit-blog-button" type="submit">Login</button>
          </div>
        </form>
        <form action="/users/register" method="GET">
          <div class="form-group">
            <button class="edit-blog-button" type="submit">Go to Registration</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
